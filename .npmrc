# Lockfile + integrity discipline
strict-peer-dependencies=false
auto-install-peers=true
prefer-frozen-lockfile=true
fund=false
audit-level=high

# Supply-chain safety
# Lifecycle scripts are allowed only for packages we trust (declared per-package).
# This blocks rogue postinstall scripts from new transitive deps.
node-options=--experimental-vm-modules
